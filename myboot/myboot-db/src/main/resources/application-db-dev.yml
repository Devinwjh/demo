spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db1
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: localhost
    password:
    port: 6379
    pool:
      max-idle: 500
      min-idle: 50
      max-active: 2000
      max-wait: 1000
    database: 5
    timeout: 3000
